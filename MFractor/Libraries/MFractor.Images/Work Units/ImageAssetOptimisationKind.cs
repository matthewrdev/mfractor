namespace MFractor.Images.WorkUnits
{
    /// <summary>
    /// When launching the image optimisation tool, the kind of task that it will perform.
    /// </summary>
    public enum ImageAssetOptimisationKind
    {
        Bulk,

        ImageAsset,

        ProjectFile,
    }
}
